# Read by docker-compose.yml. If you change this, also update DocChat/appsettings.json
# (Embeddings:Model) so the .NET app requests embeddings from the same model.
TEI_MODEL_ID=BAAI/bge-small-en-v1.5
